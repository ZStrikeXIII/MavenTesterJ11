package com.company;

import java.sql.Connection;
import java.sql.DriverManager;
import java.sql.ResultSet;
import java.sql.SQLException;
import java.sql.Statement;

/**
 * At the current moment, this class has no functionality, but it will involve the BookStore
 * database file in some way. Later iterations will most likely contain the rest of the code needed
 * to both connect the database and alter it in some way, though at the current moment it does
 * nothing.
 */
public class BookStoreDB {

}
